name: 📊 每月完整数据更新

on:
  # 定时任务：每月1号凌晨2点（北京时间10点 = UTC 2点）
  schedule:
    - cron: '0 2 1 * *'
  
  # 也可以手动触发
  workflow_dispatch:

jobs:
  full-update:
    runs-on: ubuntu-latest
    timeout-minutes: 480  # 最长8小时
    
    steps:
      - name: 📥 Checkout 代码
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: 🐍 设置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 安装依赖
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 🗑️ 删除旧数据
        run: |
          echo "删除旧数据，准备重新下载..."
          rm -rf kline_data/*
          mkdir -p kline_data
      
      - name: 📊 完整下载所有数据
        run: |
          cd stock_all
          python fetch_kline_history.py --output-dir ../kline_data --delay 0.05
        continue-on-error: false
      
      - name: 📈 显示数据统计
        run: |
          echo "=== 完整更新统计 ==="
          if [ -f kline_data/fetch_statistics.csv ]; then
            head -20 kline_data/fetch_statistics.csv
          fi
          echo "总文件数: $(find kline_data -name '*.csv' | wc -l)"
          echo "数据大小: $(du -sh kline_data)"
      
      - name: 💾 提交更新到仓库
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          git add kline_data/
          git commit -m "🔄 每月完整数据更新 - $(date '+%Y-%m-%d')"
          git push
          echo "✅ 数据已推送到仓库！"
      
      - name: 📋 生成运行报告
        if: always()
        run: |
          echo "## 🔄 每月完整数据更新报告" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**更新时间**: $(date)" >> $GITHUB_STEP_SUMMARY
          echo "**更新模式**: 完整重新下载所有数据" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f kline_data/fetch_statistics.csv ]; then
            echo "**总文件数**: $(find kline_data -name '*.csv' | wc -l)" >> $GITHUB_STEP_SUMMARY
            echo "**数据大小**: $(du -sh kline_data | cut -f1)" >> $GITHUB_STEP_SUMMARY
          fi

