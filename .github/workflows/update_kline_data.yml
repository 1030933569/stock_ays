name: 📊 每日增量更新K线数据

on:
  # 定时任务：每天下午4点（收盘后，北京时间）
  schedule:
    - cron: '0 8 * * *'  # 每天 16:00 北京时间 = 8:00 UTC
  
  # 也可以手动触发
  workflow_dispatch:

jobs:
  update-data:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 最长6小时
    
    steps:
      - name: 📥 Checkout 代码
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # 获取完整历史，方便提交
      
      - name: 🐍 设置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 安装依赖
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 📊 增量更新K线数据
        run: |
          cd stock_all
          # 增量模式：只追加新数据（快速，1-5分钟）
          python fetch_kline_history.py --output-dir ../kline_data --delay 0.05 --incremental
        continue-on-error: false
      
      - name: 📈 显示数据统计
        run: |
          echo "=== 数据统计 ==="
          if [ -f kline_data/fetch_statistics.csv ]; then
            echo "统计文件已生成"
            head -20 kline_data/fetch_statistics.csv
          fi
          echo ""
          echo "总文件数: $(find kline_data -name '*.csv' | wc -l)"
          echo "数据大小: $(du -sh kline_data)"
      
      - name: 💾 提交更新到仓库
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 添加所有更新的数据文件
          git add kline_data/
          
          # 检查是否有变更
          if git diff --cached --quiet; then
            echo "ℹ️  没有数据更新"
          else
            echo "📝 提交数据更新..."
            git commit -m "🤖 自动更新K线数据 - $(date '+%Y-%m-%d %H:%M:%S')"
            git push
            echo "✅ 数据已推送到仓库！"
          fi
      
      - name: 📋 生成运行报告
        if: always()
        run: |
          echo "## 📊 每日增量更新报告" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**更新时间**: $(date)" >> $GITHUB_STEP_SUMMARY
          echo "**更新模式**: 增量追加（只下载新数据）" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f kline_data/fetch_statistics.csv ]; then
            echo "**数据文件**: 已生成" >> $GITHUB_STEP_SUMMARY
            echo "**总文件数**: $(find kline_data -name '*.csv' | wc -l)" >> $GITHUB_STEP_SUMMARY
            echo "**数据大小**: $(du -sh kline_data | cut -f1)" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ 未找到统计文件" >> $GITHUB_STEP_SUMMARY
          fi

