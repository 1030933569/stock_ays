name: 📊 每周强制更新K线数据

on:
  # 定时任务：16:40开始，间隔50分钟，共15批（超时45分钟）
  schedule:
    - cron: '40 8 * * *'   # 第1批：16:40 北京时间（删除旧数据）
    - cron: '30 9 * * *'   # 第2批：17:30
    - cron: '20 10 * * *'  # 第3批：18:20
    - cron: '10 11 * * *'  # 第4批：19:10
    - cron: '0 12 * * *'   # 第5批：20:00
    - cron: '50 12 * * *'  # 第6批：20:50
    - cron: '40 13 * * *'  # 第7批：21:40
    - cron: '30 14 * * *'  # 第8批：22:30
    - cron: '20 15 * * *'  # 第9批：23:20
    - cron: '10 16 * * *'  # 第10批：00:10
    - cron: '0 17 * * *'   # 第11批：01:00
    - cron: '50 17 * * *'  # 第12批：01:50
    - cron: '40 18 * * *'  # 第13批：02:40
    - cron: '30 19 * * *'  # 第14批：03:30
    - cron: '20 20 * * *'  # 第15批：04:20
  
  # 也可以手动触发
  workflow_dispatch:

# 确保同一时间只有一个任务运行，避免冲突
concurrency:
  group: kline-update
  cancel-in-progress: false  # 不取消正在运行的任务，等待完成

permissions:
  contents: write  # 授予写入权限，解决 403 错误

jobs:
  update-data:
    runs-on: ubuntu-latest
    timeout-minutes: 45  # 每批最长45分钟
    
    steps:
      - name: 📥 Checkout 代码
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # 获取完整历史，方便提交
      
      - name: 🐍 设置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 安装依赖
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 🗑️ 清空旧数据（仅第1批执行）
        if: github.event.schedule == '35 8 * * *' || github.event_name == 'workflow_dispatch'
        run: |
          echo "第1批：删除旧数据，准备重新下载..."
          rm -rf kline_data/*
          mkdir -p kline_data
      
      - name: 📊 下载K线数据（断点续传）
        run: |
          cd stock_all
          # 使用断点续传，跳过已下载的股票
          # 延迟0.15秒，减少被限流风险
          python fetch_kline_history.py --output-dir ../kline_data --delay 0.2 --skip-existing
        continue-on-error: false
      
      - name: 📈 显示数据统计
        if: always()  # 即使超时/失败也执行
        run: |
          echo "=== 数据统计 ==="
          if [ -f kline_data/fetch_statistics.csv ]; then
            echo "统计文件已生成"
            head -20 kline_data/fetch_statistics.csv
          fi
          echo ""
          echo "总文件数: $(find kline_data -name '*.csv' | wc -l)"
          echo "数据大小: $(du -sh kline_data)"
      
      - name: 💾 提交更新到仓库
        if: always()  # 即使超时/失败也提交已下载的数据
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 先拉取远程最新代码（保留本地下载的数据）
          git stash push -m "temp" -- kline_data/ || true
          git pull --rebase origin main || true
          git stash pop || true
          
          # 添加所有更新的数据文件
          git add kline_data/
          
          # 检查是否有变更
          if git diff --cached --quiet; then
            echo "ℹ️  没有数据更新"
          else
            echo "📝 提交数据更新..."
            git commit -m "🤖 自动更新K线数据 - $(date '+%Y-%m-%d %H:%M:%S')"
            
            # 推送，失败则重试
            for i in 1 2 3; do
              echo "尝试推送 (第$i次)..."
              if git push; then
                echo "✅ 数据已推送到仓库！"
                break
              else
                echo "推送失败，拉取最新代码后重试..."
                git pull --rebase origin main || true
                sleep 3
              fi
            done
          fi
      
      - name: 📋 生成运行报告
        if: always()
        run: |
          echo "## 📊 K线数据更新报告" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**更新时间**: $(date)" >> $GITHUB_STEP_SUMMARY
          echo "**更新模式**: 分15批断点续传（间隔30分钟，第1批删除旧数据）" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f kline_data/fetch_statistics.csv ]; then
            echo "**数据文件**: 已生成" >> $GITHUB_STEP_SUMMARY
            echo "**总文件数**: $(find kline_data -name '*.csv' | wc -l)" >> $GITHUB_STEP_SUMMARY
            echo "**数据大小**: $(du -sh kline_data | cut -f1)" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ 未找到统计文件" >> $GITHUB_STEP_SUMMARY
          fi

